fastapi>=0.115
uvicorn>=0.30
python-docx>=1.2.0
python-pptx>=1.0.2
python-multipart
pyyaml
openai>=1.40        # OpenAI 兼容客户端，用于调用百炼 DashScope
docxtpl           # 公文模板渲染
# 后续 skill 需要时再启用：
# dashscope         # 百炼原生 SDK（录音转写/CosyVoice）
# redis rq          # 任务队列（并发量上来后启用）
